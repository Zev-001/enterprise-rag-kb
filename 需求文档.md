# 企业知识库问答系统（RAG）—— 需求与架构

> 阶段 5 · 项目 1。面向**零基础转行**同学，用「能跑、能演示、能写进简历」为标准。
> 一句话目标：**把公司文档传上去，员工用大白话提问，系统基于文档回答并标出来源，绝不瞎编。**

---

## 一、它要解决什么痛点（人话版）

企业里文档一大堆：员工手册、产品说明书、制度文件、FAQ……但没人愿意一页页翻。
传统搜索只能按关键词返回「一串文件链接」，还得自己读。

这个系统做的是：**你直接问「年假怎么请」「报销上限多少」「这个接口怎么调」，
它读遍所有文档，把答案用一句话告诉你，并标出「这句来自《员工手册》第 3 段」**。

关键卖点 = **可溯源、不幻觉**。资料里没有就老实说「资料里没提到」，
而不是像普通聊天机器人那样凭训练记忆编一个听起来对、实际错的答案。
（这正是 day17 dailyrag 验证过的防幻觉纪律，本项目直接复用。）

---

## 二、整体架构（数据流）

```
上传文档(PDF/Word/TXT/MD)
      │
      ▼
[解析 ingest_docs]  → 抽出纯文字（扫描件 PDF 需另接 OCR）
      │
      ▼
[切块 chunk_text]   → 按 ~320 字一段切，长段留 64 字重叠，保证语义不断
      │
      ▼
[向量化+索引]       → 语义：bge-small-zh + faiss（默认）
                     → 兜底：TF-IDF + 中文二元组（不装依赖也能跑）
      │
      ▼
knowledge_base.jsonl  ← 带 doc_id / filename / title / chunk_id 的检索库
      │
      ▼
[提问] → [retrieve 检索 Top-K] → [拼上下文+出处编号] → [LLM 基于资料作答]
      │
      ▼
Web 界面：答案 + 可点击来源卡片（哪份文件、相关度多少、原文片段）
```

---

## 三、技术选型与理由（为什么这么选）

| 组件 | 选型 | 理由 |
|---|---|---|
| Web 框架 | **Flask** | 轻量、概念少、好部署；比 FastAPI 更适合零基础读得懂 |
| 前端 | **原生 HTML/CSS/JS** | 不引入 npm/打包，界面逻辑一眼看明白，便于演示和改 |
| 语义向量 | **bge-small-zh-v1.5** | 中文效果好的小模型，512 维，~90MB，个人电脑跑得动 |
| 向量库 | **faiss-cpu** | 内存索引足够本项目规模；崩了自动回落 TF-IDF，离线可用 |
| 大模型 | **DeepSeek** | 已有 API key，便宜；回答用「只据资料」硬约束防幻觉 |
| 文档解析 | pdfplumber + python-docx | 覆盖最常见的企业文档格式 |

**为什么不直接用 day17 的 dailyrag？**
dailyrag 元数据写死成 `(date, source, url, title)`，只适合「按天归档的新闻」。
企业文档用 `(doc_id, filename, title)` 更自然。检索/防幻觉/事实核查算法是通用的，原样复用，只改数据模型 + 加多知识库隔离。

---

## 四、功能清单（MVP）

- [x] 多格式文档上传（PDF / Word / TXT / Markdown）
- [x] 自动切块入库，带文件名/标题溯源元数据
- [x] Web 聊天界面：提问 → 基于文档作答
- [x] 答案带**可点击来源卡片**（文件名 + 相关度 + 原文片段）
- [x] 防幻觉纪律：资料没有就说「资料里没提到」
- [x] 双检索后端，离线（TF-IDF）也能跑通
- [x] 知识库状态展示（几份文档 / 几个块）

## 五、演示素材建议

用 dailyrag 已有的 AI 新闻 `news_2026-08-27.jsonl`（6 篇真实资讯）当演示「公司文档」，
提问「阿里开源的 Qwen 模型参数是多少」「Kimi K3 上下文多长」等，
即可展示「基于资料作答 + 标来源 + 不编」的完整效果。

---

## 六、后续可扩展（面试能聊的点，不是 MVP 必须）

1. **多知识库隔离**：按部门/客户分 namespace，权限控制谁能问哪个库
2. **流式输出**：回答逐字吐出，体验更接近真聊天
3. **重排序 rerank**：召回 Top-K 后再用 cross-encoder 精排，提升命中质量
4. **OCR 接入**：扫描件 PDF 也能读
5. **部署上线**：用 Render / 服务器把 5000 端口暴露成公网，做成可分享链接
6. **评估集**：造一批「问题-标准答案」对，量化回答准确率，证明系统可靠
